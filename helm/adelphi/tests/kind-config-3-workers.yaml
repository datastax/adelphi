# 3-worker cluster config
kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
containerdConfigPatches:
- |-
  [plugin."io.containerd.grpc.v1.cri".registry.configs."*".tls]
    insecure_skip_verify = true
nodes:
- role: control-plane
- role: worker
- role: worker
- role: worker