name: adelphi Python

on:
  push:
    branches: [ 115-breadth-first-testing ]
  pull_request:
    branches: [ 115-breadth-first-testing ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python 3.x
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'
    - name: Install dependencies
      run: |
        cd python
        pip install ./adelphi
        test-adelphi -c 2.1.22 -p py2
